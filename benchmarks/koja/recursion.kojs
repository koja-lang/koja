fn fib(n: Int) -> Int
  if n <= 1
    n
  else
    fib(n - 1) + fib(n - 2)
  end
end

t0 = Instant.now()
r = fib(35)
elapsed = t0.elapsed().to_milliseconds()
"recursion_ms #{elapsed} result #{r}".print()
