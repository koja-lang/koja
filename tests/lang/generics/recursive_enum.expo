enum Tree
  Leaf(Int)
  Branch(Tree, Tree)
end

fn sum(t: Tree) -> Int
  match t
    Tree.Leaf(v) -> v
    Tree.Branch(l, r) -> sum(l) + sum(r)
  end
end

fn main
  left = Tree.Leaf(1)
  right = Tree.Branch(Tree.Leaf(2), Tree.Leaf(3))
  t = Tree.Branch(left, right)

  print(sum(t))
end
