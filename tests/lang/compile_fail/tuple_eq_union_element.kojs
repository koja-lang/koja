# Union values cannot be compared for equality, so comparing tuples
# that contain one is rejected.
struct Cat
  name: String
end

struct Dog
  name: String
end

type Pet = Cat | Dog

pet: Pet = Cat{name: "Whiskers"}
((pet, 1) == (pet, 1)).print()
