struct Config
  name: String
end

fn consume(c: Config) -> String
  c.name
end

fn borrow(c: Config) -> String
  c.name
end

c1 = Config{name: "test"}
borrow(c1)
c1.name.print()

c2 = Config{name: "test2"}
consume(c2)
"done".print()
