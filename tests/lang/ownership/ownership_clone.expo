struct Point
  x: Int
  y: Int
end

fn consume(move p: Point) -> Int
  p.x
end

fn main
  p = Point{x: 10, y: 20}
  q = p.clone()
  print(consume(q))
  print(p.x)

  n = 42
  m = n.clone()
  print(m)
  print(n)
end
