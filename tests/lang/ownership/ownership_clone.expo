struct Point
  x: Int
  y: Int
end

fn consume(move p: Point) -> Int
  p.x
end

fn main
  p = Point{x: 10, y: 20}
  q = p.clone()
  consume(q).print()
  p.x.print()

  n = 42
  m = n.clone()
  m.print()
  n.print()
end
