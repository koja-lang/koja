struct Packet
  data: String
end

fn consume(p: Packet, handler: fn (move Packet) -> String) -> String
  handler(p)
end

fn main
  p = Packet{data: "hello"}

  result =
    consume(p, fn (move pkt: Packet) -> String pkt.data end)
  result.print()
end
