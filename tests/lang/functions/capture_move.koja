struct Config
  scale: Int
  offset: Int
end

fn apply(x: Int, f: fn (Int) -> Int) -> Int
  f(x)
end

fn main
  c = Config{scale: 3, offset: 100}

  transform =
    fn (x: Int) -> Int x * c.scale + c.offset end

  transform(5).print()
  apply(10, transform).print()
end
