struct Packet
  data: String
end

fn consume(p: Packet, handler: fn (Packet) -> String) -> String
  handler(p)
end

fn main
  p = Packet{data: "hello"}

  result =
    consume(p, fn (pkt: Packet) -> String pkt.data end)
  result.print()
end
