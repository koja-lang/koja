struct Counter
  n: Int

  fn count_down(move self)
    if self.n <= 0
      self.n.print()
      return
    end

    Counter{n: self.n - 1}.count_down()
  end
end

fn main
  Counter{n: 100000}.count_down()
end
