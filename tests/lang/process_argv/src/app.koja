alias Process.Step
alias Process.StopReason

enum AppMsg
  Noop
end

struct App: Process<List<String>, AppMsg, String>
  args: List<String>

  fn start(config: List<String>) -> Result<Self, StopReason>
    Result.Ok(App{args: config})
  end

  fn handle(self, msg: AppMsg, from: Option<ReplyTo<String>>) -> Step<Self>
    Step.Continue(self)
  end

  fn run(self) -> StopReason
    for arg in self.args
      IO.puts(arg)
    end

    StopReason.Normal
  end
end
