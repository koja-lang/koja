alias ExpoAST.Position
alias ExpoAST.Span
alias ExpoAST.Token
alias ExpoAST.TokenKind

fn main
  p = Position{offset: 0, line: 1, column: 1}
  s = Span.new(p, p)
  token = Token{kind: TokenKind.IntLit("42"), span: s}
  IO.puts("#{token.kind}")
end
