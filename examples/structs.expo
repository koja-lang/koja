struct Point
  x: Int32
  y: Int32
end

extend Point
  fn distance_squared(self) -> Int32
    self.x * self.x + self.y * self.y
  end
end

fn main
  p = Point{x: 3, y: 4}
  p.distance_squared().print()
  "x = #{p.x}, y = #{p.y}".print()
end
