const MAX_SIZE = 100
const PI = 3.14159
const GREETING = "hello"
const DEBUG = false

fn circumference(radius: Float) -> Float
  2.0 * PI * radius
end

IO.puts("Max size: #{MAX_SIZE}")
IO.puts("Circumference: #{circumference(5.0)}")

if DEBUG
  IO.puts("debug mode enabled")
end
