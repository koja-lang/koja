[package]
name = "expo-test"
description = "Test discovery + harness synthesis for the Expo compiler"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
expo-ast = { path = "../expo-ast" }
expo-parser = { path = "../expo-parser" }
