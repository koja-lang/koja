[package]
name = "expo-fmt"
description = "Opinionated code formatter for the Expo language"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
expo-ast = { path = "../expo-ast" }
expo-parser = { path = "../expo-parser" }
