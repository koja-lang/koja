[package]
name = "expo-runtime"
description = "Lightweight process runtime for the Expo language"
edition.workspace = true
license.workspace = true
version.workspace = true

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
backtrace = "0.3"
libc = "0.2"

[build-dependencies]
cc = "1"
