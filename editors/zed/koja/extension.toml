id = "koja"
name = "Koja"
description = "Syntax highlighting and language-server support for the Koja programming language."
version = "0.1.0"
authors = ["Henry Popp <hello@hpopp.dev>"]
schema_version = 1

# The grammar lives in a sibling repository. During development we
# point at the local checkout; once `tree-sitter-koja` is published
# this should switch to a public `https://...` URL with the same
# pinned commit.
[grammars.koja]
repository = "file:///Users/hpopp/Work/koja-lang/tree-sitter-koja"
commit = "f529b999e4e7c6675604ea8e1c5ecbb4c9f7a844"

[language_servers.koja-lsp]
name = "Koja LSP"
languages = ["Koja"]
